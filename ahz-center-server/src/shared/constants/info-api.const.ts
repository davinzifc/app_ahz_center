import { env } from "process";

export const APP = {
    API: {
        api: `API DHZ: Servicio de gestion y almacenamiento de informacion`,
        name: `Centro de deteccion temprana para la perdida de memoria`,
        email: env.MAIL_USER,
        info: `(Pendiente)`
    }
}